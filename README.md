# Termux Setup

An automated setup script for configuring a productive development environment in Termux on Android.

## Installation

To set up your Termux environment, run the following command:

```bash
curl -fsSL https://raw.githubusercontent.com/BibekBhusal0/termux-setup/main/termux-setup.sh | bash
```
